---
'@cloudflare/workers-oauth-provider': minor
---

Add a capability-aware OAuth storage contract with built-in Workers KV, D1, and Durable Object SQLite adapters. Workers KV remains the default, while request-scoped providers can advertise and enforce explicit consistency guarantees.
