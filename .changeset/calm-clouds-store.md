---
'@cloudflare/workers-oauth-provider': minor
---

Add a capability-aware OAuth storage contract and a legacy-compatible Workers KV adapter. Workers KV remains the default, while custom providers can use request-scoped domain operations and explicit consistency guarantees.
