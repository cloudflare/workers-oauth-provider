---
'@cloudflare/workers-oauth-provider': minor
---

Add a capability-aware OAuth storage contract with built-in Workers KV, D1, Durable Object SQLite, injected-client PostgreSQL, and scripted Redis adapters. Workers KV remains the default, while request-scoped providers can advertise and enforce explicit consistency guarantees.
